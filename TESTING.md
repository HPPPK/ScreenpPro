# ScreenPro Windows 验收矩阵

## 自动验证

~~~powershell
rtk cargo fmt --manifest-path src-tauri/Cargo.toml --check
rtk cargo test --manifest-path src-tauri/Cargo.toml
rtk cargo build --manifest-path src-tauri/Cargo.toml
rtk bun run build
~~~

Rust tests cover project serialization, templates, secret verification, shortcut validation, CDP websocket normalization, and physical-to-logical DPI conversion.网页截图现在会复用同一个无头 Edge/Chrome 进程，并在应用退出时清理；前端按 URL + 宽度缓存缩略图，编辑器默认不自动联网。

## 多显示器现场测试

在 Windows 连接两个或更多显示器后：

1. 在每个显示器设置不同分辨率或缩放比例（例如 100% + 125%，或 125% + 150%）。
2. 启动 ScreenPro 屏保，确认每块屏幕均出现独立窗口。
3. 在每块屏幕尝试拖动、缩放、最大化、最小化和关闭窗口；窗口必须恢复到对应显示器的完整物理边界。
4. 按任意键唤起解锁层，在任意屏幕输入密码；所有屏保窗口必须一起关闭，工作台恢复到原位置和尺寸。
5. 在屏保期间改变主显示器、拔出副显示器，再观察窗口是否能安全恢复；如果系统改变显示器布局，重新启动屏保进行一次完整验证。

## Windows 窗口切换与系统入口

- Win + Tab / 任务视图：屏保窗口不能被普通关闭绕过密码。
- Alt + Tab：屏保失焦后应重新置顶，不能通过缩放看到底层工作台。
- Win + D、点击任务栏、点击工作台预览：主工作台不能绕过密码显示或关闭屏保。
- 尝试移动/调整屏保窗口：resizable=false、固定尺寸约束和事件重申逻辑必须恢复窗口。
- Ctrl + Alt + Delete、任务管理器、管理员强制终止：这些是 Windows 系统安全入口，ScreenPro 不承诺拦截。

## 当前机器现场结果（2026-07-23）

当前环境检测到 1 个显示器：DISPLAY1，物理尺寸 1920 × 1080，主屏。多显示器、不同 DPI 和任务视图的真实结果需要在对应硬件上执行，不能用单屏构建结果冒充通过。

## 预期结果

任何组件加载失败只能影响自身组件；屏保主体、工作台和其他组件不能白屏或卡死。网络组件应显示错误并保留上次成功数据，编辑器预览默认不主动发起网络请求。

## 稳定化回归清单

- 编辑器连续修改文字、拖动组件：停止操作约 700ms 后自动保存；保存过程中编辑器不关闭，修改期间不会重复刷新整个工作台。
- 保存失败：编辑器保持打开，显示“保存失败，请重试”，当前内存编辑内容不能丢失。
- 图片组件：分别验证正常图片、移动原文件后的托管图片、损坏图片；损坏图片应显示错误和“重新选择”，不能白屏。
- `cover` / `contain`：图片、照片墙、网页缩略图分别验证裁切填充与完整显示，且编辑器和屏保运行页一致。
- 网络组件：断网、HTTP 错误、超时、切换 URL；运行页显示组件级错误并保留上次成功数据，编辑器不自动请求 GitHub/RSS/天气。
- 网页预览：首次生成、重试、相同 URL 重复生成、运行页刷新；相同 URL/尺寸应命中缓存，不应每次启动新的 Edge/Chrome 进程。
- 属性面板：文字字重、时钟/世界时钟对齐、二维码颜色、天气对齐、照片墙移除照片、Markdown 最大行数均应影响实际渲染。
- 属性默认值：修改组件内容、颜色、字号和适配方式后点击“恢复属性默认值”，只恢复 props；位置、尺寸、名称、图层、锁定和隐藏状态应保持不变。
- 右键菜单：画布组件和图层行右键均能打开菜单；复制、删除、前移/后移、置顶/置底、锁定/解锁、隐藏/显示和恢复默认值应作用于右键选中的组件；点击空白处或菜单外应关闭菜单。
- 组件实际效果：倒计时空目标显示“目标时间无效”而不是误报结束；未知世界城市显示“未知时区”；二维码内容为空或颜色/尺寸异常时显示组件级错误且不越界；照片墙空资源、Markdown 超长内容、图片加载失败均不能让画布白屏。

## 第三优先级编辑器验收

- 图层面板：验证上移、下移、置顶、置底，多选后操作应保持选中组件的相对顺序。
- 复制与删除：使用工具栏、`Ctrl/Cmd + D` 和 `Delete` 验证；锁定组件不能被拖动、删除或键盘移动。
- 键盘移动：方向键移动 1 个逻辑像素，`Shift + 方向键` 移动 10 个逻辑像素。
- 对齐与吸附：打开网格吸附，拖动组件接近网格、画布边缘和其他组件边缘/中心，橙色对齐线应出现且位置稳定。
- 撤销/重做：修改属性、拖动、排序、复制、删除后分别使用 `Ctrl/Cmd + Z`、`Ctrl/Cmd + Shift + Z` 或 `Ctrl/Cmd + Y`。
- 画布缩放：测试 50% 到 150%，确认缩放只影响编辑器视图，保存的逻辑坐标和屏保运行效果不被改变。
- 锁定/隐藏/命名：锁定后不能拖动或删除；隐藏后运行页不渲染；自定义名称应在图层面板和属性面板保留。
